services:
  app-server:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "4500:4500"
    volumes:
      - letta-state:/root/.letta
      - letta-workspace:/workspace

volumes:
  letta-state:
  letta-workspace:
