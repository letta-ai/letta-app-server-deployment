services:
  computer:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - letta-state:/root/.letta
      - letta-workspace:/workspace

volumes:
  letta-state:
  letta-workspace:
